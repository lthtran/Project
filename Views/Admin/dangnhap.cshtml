@*
    @model WebsiteThuCungBento.Models.DangNhapModel
    @{
        <title>Đăng Nhập Admin</title>
        Layout = null;
    }

    <!DOCTYPE html>

    <html>
    <head>
        <link rel="shortcut icon" href="~/img/logo/pet2.png" type="image/png" />
        <meta name="viewport" content="width=device-width" />
    </head>
    <body>
        <link href="~/Content/dangnhapadmin.css" rel="stylesheet" />
        <script language="javascript" src="~/Scripts/dangnhapadmin.js"></script>
        @*<div id="back">
            <canvas id="canvas" class="canvas-back"></canvas>
            <div class="backRight">
            </div>
            <div class="backLeft">
                <img class="product-image-feature" src="@Url.Content("~/img/pet960x1050.jpg")" />
            </div>
        </div>

                <div class="justify-content-center">
                    <div class="content">
                        <h2 style="text-align:center;">Đăng Nhập</h2>
                        <h1 style="text-align:center;">Tài khoản Admin - Nhân viên quản lý sản phẩm</h1>
                        <h4 style="color:red;">@ViewBag.Thongbao</h4>
                        @using (Html.BeginForm("dangnhap", "Admin", FormMethod.Post))
                        {
                            @Html.AntiForgeryToken()
                            <div class="form-element form-stack">
                                <div style="color:red;font-size:small;text-align:initial">
                                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                                </div>
                                <strong>@Html.LabelFor(model => model.tendn, htmlAttributes: new { @class = "control-label col-md-2" })</strong>
                                <div style="color:red;font-size:small;text-align:initial">
                                    @Html.ValidationMessageFor(model => model.tendn, "", new { @class = "text-danger" })
                                </div>
                                <input class="form-control text-box single-line input-full " data-val="true" data-val-required=" Tên đăng nhập không được để trống. " id="tendn" name="tendn" type="text" value="">
                            </div>
                            <div class="form-element form-stack">
                                <strong>@Html.LabelFor(model => model.matkhau, htmlAttributes: new { @class = "control-label col-md-2" })</strong>
                                <div style="color:red;font-size:small;text-align:initial">
                                    @Html.ValidationMessageFor(model => model.matkhau, "", new { @class = "text-danger" })
                                </div>
                                <input class="form-control text-box single-line input-full" data-val="true" data-val-length="Độ dài mật khẩu ít nhất 6 - 20 ký tự. " data-val-length-max="20" data-val-length-min="6" data-val-required="Mật khẩu không được để trống. " id="matkhau" name="matkhau" type="password" value="">
                            </div>
                            <div class="form-element form-submit">
                                <button class="login" type="submit" name="dangnhap">Đăng Nhập</button>
                            </div>
                        }

                    </div>
                </div>


    </body>
    </html>*@
@model WebsiteThuCungBento.Models.DangNhapModel
@{
    <title>Đăng Nhập Admin</title>
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="~/img/logo/pet2.png" type="image/png" />
    <link href="~/Content/dangnhapadmin.css" rel="stylesheet" />
    <script language="javascript" src="~/Scripts/dangnhapadmin.js"></script>
    <!-- Add FontAwesome for the eye icon -->
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/fontawesome.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">

    <style>
        /* Toàn bộ màn hình nền */
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Khung đăng nhập */
        .login-container {
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 40px;
            border-radius: 8px;
            width: 100%;
            max-width: 400px;
        }

            /* Tiêu đề */
            .login-container h2 {
                text-align: center;
                font-size: 24px;
                color: #333;
                margin-bottom: 20px;
            }

            .login-container h1 {
                text-align: center;
                font-size: 16px;
                color: #666;
                margin-bottom: 20px;
                font-weight: 400;
            }

        /* Nhãn */
        label {
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 8px;
            display: block;
        }

        /* Input */
        .form-control {
            width: 100%;
            padding: 12px 15px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 14px;
        }

        /* Nút đăng nhập */
        .login {
            width: 100%;
            background-color: #007bff;
            color: white;
            padding: 12px 15px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            .login:hover {
                background-color: #0056b3;
            }

        /* Thông báo lỗi */
        .text-danger {
            color: red;
            font-size: 12px;
            margin-top: -15px;
        }

        /* Eye icon for password toggle */
        .input-group {
            position: relative;
        }

            .input-group i {
                position: absolute;
                top: 50%;
                right: 15px;
                transform: translateY(-50%);
                cursor: pointer;
                color: #aaa;
            }
    </style>

    <script>
        function togglePasswordVisibility() {
            var passwordField = document.getElementById("matkhau");
            var eyeIcon = document.getElementById("eyeIcon");
            if (passwordField.type === "password") {
                passwordField.type = "text";
                eyeIcon.classList.remove("fa-eye");
                eyeIcon.classList.add("fa-eye-slash");
            } else {
                passwordField.type = "password";
                eyeIcon.classList.remove("fa-eye-slash");
                eyeIcon.classList.add("fa-eye");
            }
        }
    </script>
</head>
<body>

    <div class="login-container">
        <h2>Đăng Nhập</h2>
        <h1>Tài khoản Admin - Nhân viên quản lý sản phẩm</h1>
        @using (Html.BeginForm("dangnhap", "Admin", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div class="form-element form-stack">
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <label for="tendn">Tên đăng nhập:</label>
                @Html.ValidationMessageFor(model => model.tendn, "", new { @class = "text-danger" })
                <input class="form-control" style="margin-top: 10px" id="tendn" name="tendn" type="text" placeholder="Nhập tên đăng nhập">

                <label for="matkhau">Mật khẩu:</label>
                @Html.ValidationMessageFor(model => model.matkhau, "", new { @class = "text-danger" })

                <!-- Password input with toggle eye icon -->
                <div class="input-group">
                    <input class="form-control" style="margin-top: 10px" id="matkhau" name="matkhau" type="password" placeholder="Nhập mật khẩu">
                    <i id="eyeIcon" style="margin-top: -5px" class="fas fa-eye" onclick="togglePasswordVisibility()"></i>
                </div>
            </div>

            <h4 style="color:red; text-align: center;">@ViewBag.Thongbao</h4>

            <div class="form-element form-submit">
                <button class="login" type="submit" name="dangnhap">Đăng Nhập</button>
            </div>
        }
    </div>

</body>
</html>

